services:
  app:
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "8091:8000"
    volumes:
      - ./backend/data:/app/backend/data
    environment:
      - DATABASE_URL=sqlite:///./backend/data/luckydraw.db
    restart: always
