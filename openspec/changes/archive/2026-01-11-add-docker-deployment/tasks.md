## 1. Preparation
- [x] 1.1 创建 `.dockerignore` 文件，排除 `node_modules`, `dist`, `.git` 等目录。
- [x] 1.2 编写 `nginx.conf` 生产环境配置文件。

## 2. Dockerization
- [x] 2.1 编写 `Dockerfile`，实现多阶段构建。
- [x] 2.2 在本地进行镜像构建测试。

## 3. Orchestration
- [x] 3.1 编写 `docker-compose.yml` 文件。
- [x] 3.2 使用 `docker-compose up` 进行集成测试。

## 4. Documentation
- [x] 4.1 在 `README.md` 中增加 Docker 部署说明。
